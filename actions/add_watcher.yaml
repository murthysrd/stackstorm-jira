---
name: add_watcher
runner_type: python-script
description: Add a watcher to a particular JIRA issue.
enabled: true
entry_point: add_watcher.py
parameters:
  issue_key:
    type: string
    description: Issue key (e.g. PROJECT-1000).
    required: true
  value:
    type: string
    description: the field text itself.
    required: true
